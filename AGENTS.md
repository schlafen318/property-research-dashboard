# Repository workflow

- After an implementation passes the repository's required verification, push the branch, create and merge a pull request, and deploy it.
- Do not ask which integration option to use or request separate deployment approval for this repository unless deployment is blocked or the action would be destructive beyond the requested change.
